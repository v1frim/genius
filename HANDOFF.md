# HANDOFF — «ГЕНІЙ», тренажер геніальності

Стан на 2026-06-10. Усе описане нижче змерджено в `main` і задеплоєно.

## Що це

Особистий сайт-тренажер користувача (v1frim) для щоденного розвитку: увага, швидкочитання,
друк, дикція, усний рахунок, пам'ять, медитація + трекер цілей і щоденних задач.
Повністю статичний (HTML/CSS/JS без збірки), працює офлайн, дані в localStorage.

- **Репозиторій**: https://github.com/v1frim/genius
- **Сайт**: https://v1frim.github.io/genius/ (GitHub Pages, deploy from branch `main`, root `/`)
- **Мова UI**: українська. Тема: фіолетова, у стилі logiclike.com (referenced користувачем як зразок).

## Робочий процес (домовленості з користувачем)

1. Розробка ведеться в гілці **`claude/ecstatic-goodall-3xlglc`** → push → PR у `main` → merge.
2. **Користувач дав дозвіл самостійно створювати і мерджити PR** («даю добро»).
3. Після мерджу Pages перебудовується автоматично за ~1 хв (workflow «pages build and deployment»).
4. Користувач надсилає скриншоти із зауваженнями → виправляємо ітеративно.
5. Кожна зміна перевіряється Playwright-тестами перед пушем (див. «Тестування»).

Історія PR: #1 початковий сайт · #2 фікси Шульте · #3 RSVP-квіз+Вікіпедія, Шульте СТОП/кольори
· #4 око в центрі · #5 проза+5 кольорів рівнів · #6 більша таблиця + рерайт друку · #7 Space/Tab + cloze-квіз + 93 уривки.

## Архітектура

```
index.html          — каркас, підключення скриптів по порядку (без модулів/збірки)
css/style.css       — уся тема; CSS-змінні в :root; класи tier-* для 5 рівнів оцінки
js/store.js         — стан + localStorage (ключ genius.v1), rollover дня/тижня, рекорди
js/ui.js            — h() створення DOM, тости, спарклайни (SVG), beep (WebAudio), shuffle/rnd
js/data.js          — задачі/цілі за замовчуванням, скоромовки + банки генератора,
                      тексти для друку (UA панграми + EN), 5 текстів для тесту читання з питаннями
js/data.prose.js    — корпус художніх уривків для RSVP (зараз 503; нарощувати батчами тут)
js/modules/*.js     — кожен тренажер = модуль {id, title, icon, render(root) → cleanup()}
js/app.js           — роутер (#/id), навігація, глобальні клавіші Space/Tab, App.primaryAction
```

Патерн модуля: `render(root)` будує DOM через `App.ui.h`, повертає `cleanup` (зняти інтервали/лісенери).
Новий модуль: створити файл, додати `<script>` в index.html, додати в `MODULES` в app.js.

### Ключові механіки

- **ЗІГРАНИЙ час за сьогодні** (НЕ присутність на сайті — користувач явно відмовився від
  presence-обліку): `store.timeByDay[date][sectionId]=ms`; модулі викликають
  `App.store.addTime(розділ, ms)` У МОМЕНТ ЗАВЕРШЕННЯ гри/сесії з реальною тривалістю:
  schulte — finish/finishMarathonGame (ms гри); reading — finish (activeMs), частковий стоп
  Esc (activeMs), тест (readMs); typing — finish (elapsed; УВАГА: зчитувати elapsedMs() ДО
  finished=true, інакше живий відрізок обнуляється — був баг); arithmetic — finish;
  memory — обидві гри міряють сесію від СТАРТ до finishSession; twisters — час показу
  скоромовки до «прочитав ×3» (стеля 4 хв); meditation — addEntry (minutes×60000).
  Роутер лише малює: бейдж `#sectionTime` «⏱ зіграно сьогодні» (top-right, ХОВАЄТЬСЯ на
  не-тренажерах — TRAINER_IDS у app.js) і суму в сайдбарі #totalTime «⏱ зіграно сьогодні»
  (`store.timeToday()` без аргументу = сума), оновлення інтервалом 1.5 с. У Шульте додатково
  відновлено рядок у hud «🕐 сьогодні в Шульте» (updateToday через updateInfo).
  CSS: .schulte-grid { flex-shrink:0 } і .schulte-hud { width:150px } — інакше довгий текст
  hud стискає сітку (був регрес 620→486). Зберігається в localStorage ~раз на 8 с + на
  visibilitychange/beforeunload. УВАГА для тестів: через beforeunload-save шаблон «правити
  localStorage напряму + reload» більше НЕ працює (старий стан клобрить правку) — у тестах
  став префи через `App.store.setPref(...)` і навігуй через `location.hash` без reload.
- **store.rollover()**: при зміні дати — вчорашні щоденні задачі йдуть в `taskHistory` (pct),
  чекбокси скидаються; щотижневі скидаються по понеділках. Викликається при кожній навігації.
- **Стрік**: день зараховано при ≥70% щоденних задач (PASS_PCT у dashboard.js).
- **Цілі з auto**: рекорди підтягуються в цілі (reading тест wpm, typing cpm, медитації count/max,
  digit span, visual matrix). `autoMode: "max"` → max(base, авто); `"count"` → base + кількість.
  RSVP-записи НЕ впливають на рекорд читання (поле `wpm` тільки у kind:"test"; у RSVP — `avgWpm`).
- **App.primaryAction**: модуль реєструє функцію → глобальний Space її викликає (старт гри).
  Скидається роутером при навігації. `stopImmediatePropagation` щоб старт-Space не паузив RSVP.
  Tab/Shift+Tab — перемикання розділів (perventDefault на Tab глобально).

### Специфічні рішення в модулях

- **Шульте**: сітка 620px (~90% logiclike), шрифт `calc(var(--fs) * 1cqw)` пропорційний клітинці.
  На непарних розмірах центральна клітинка = око (SVG, EYE_SVG), чисел n²−1; на парних — око-накладка
  `.center-eye`. Знайдені числа затемнюються (не зникають). Кнопка СТОП. Оцінка evaluate():
  пороги n*0.8/1.2/1.8/2.6 c → класи tier-elite/great/good/mid/slow (5 різних кольорів).
  Орієнтири рендеряться стовпчиком під вибраний розмір. Режими: перемішування після кліку (s),
  зворотний (r); ключ режиму — рядок "s"/"r"/"sr"/"". Розміри перемикаються Shift'ами:
  ЛІВИЙ — наступний, ПРАВИЙ — попередній (з wrap'ом). Таймер/помилки/СТОП — у колонці
  .schulte-hud ліворуч від сітки (на вузьких екранах згортається в рядок над сіткою);
  єдина кнопка СТАРТ — в оверлеї таблиці. Час у розділі показує універсальний бейдж (див. вище).
  Режим «Марафон 3×3 → 7×7»: чекбокс + повзунок «ігор на розмір» (2–10, pref schulte.marathonGames);
  проходиш усі розміри по черзі, між іграми інтерстиціал з авто-продовженням (1.4 с, Space — далі).
  КОЖНА гра марафону зараховується ОКРЕМИМ записом {size, mode, timeMs, errors, mar:true} (з'являється
  в результатах рядком із міткою 🏁); СТОП посеред марафону лишає всі вже завершені ігри (поточна
  незавершена не пишеться). Наприкінці — оверлей із сумарним часом (НЕ окремий запис). Старі сумарні
  записи {marathon:true, games} ще читаються в evaluateRec/таблиці для зворотної сумісності.
- **Швидкочитання (RSVP)**: джерела — «Художні» (дефолт, з data.prose.js, склейка випадкових
  уривків до довжини s/m/l = 130/280/550 слів) та «Вікіпедія» (uk.wikipedia.org REST
  /page/random/summary, батчами по 4, фільтр заглушок <20 слів). ORP (червона літера-якір) —
  опція, вимкнена за замовчуванням (слова просто по центру — прохання користувача).
  Плейсхолдер «приклад» показує розмір шрифту наживо. Після тексту — квіз: 3 питання РІЗНОГО
  стилю: (1) пізнавання речення, (2) cloze «яке слово пропущено» (дистрактори зі схожим
  закінченням з невикористаних уривків — менш вгадувані), (3) про дійову особу: properNouns()
  шукає власні назви (велика літера не на початку речення) → «Чиє ім'я/назва?»; якщо назв нема —
  subjectQuestion() за ключовими словами «про кого йшлося». Режим «Адаптація темпу» (pref
  rsvp.adapt): темп наступного тексту = +10 (0 помилок)/0 (1)/−10 (2)/−20 (3) сл/хв; вимагає
  ввімкнених питань (вмикає автоматично, знімається разом із ними). comp зберігається в запис;
  ефективна швидкість = wpm×comp. «Тест швидкості» (вкладка) — 5 текстів з ручними питаннями.
- **Клавотренажер**: друк «крізь помилки» — хибний символ червоний, виправлення Backspace,
  фініш лише коли все правильно. Нормалізація: ґ↔г, усі тире → "-", всі апострофи → "'",
  лапки «»„“ → '"'. Введення через keydown на прихованому input (input-події глушаться).
  Таймер на паузі коли фокус втрачено (accumMs + runStart). Короткий свій текст не підміняється.
- **Скоромовки**: 38 у колекції + генератор з алітераційних банків (15 літер).
- **Арифметика**: 5 рівнів, операції +−×÷ (chips), режими спринт 60/120, 20 прикладів, без ліміту.
- **Пам'ять**: digit span (адаптивний, 10 раундів, прямий/зворотний) + зорова матриця (3 життя).
- **Медитація**: пейсер (бокс 4-4-4-4, сон 4-7-8, когерентне 5-5, свій), таймер сесій,
  журнал (+ ручне додавання), прогрес до 1000 медитацій і 180 хв.
- **Дані**: експорт/імпорт JSON, повне скидання.

## Тестування

Інструменти в контейнері: node v22, playwright (глобально, `/opt/node22/lib/node_modules/playwright`,
chromium у `/opt/pw-browsers`), http-server. Зовнішня мережа з контейнера ЗАКРИТА (allowlist) —
Вікіпедія тестується перехопленням `page.route('**/api/rest_v1/page/random/summary')` з моком.

Схема перевірки перед пушем:
1. `node --check` на кожен змінений js-файл.
2. Node-smoke логіки store/data зі стабами window/localStorage.
3. Playwright-сценарії: підняти `npx http-server -p 8123`, проклацати модуль як користувач,
   перевірити localStorage-записи і відсутність pageerror. Кирилицю вводити НЕ через
   keyboard.type (не шле keydown), а dispatchEvent(new KeyboardEvent('keydown', {key})).
Тестові скрипти жили в /tmp (ефемерні) — за потреби писати заново за цією схемою.

## Початковий запит користувача (основа всього проєкту)

Користувач хоче сайт, де розвиватиме свою «геніальність». Чим займається на момент старту:

1. **Таблиці Шульте** різної складності (раніше на logiclike.com/game/schulte — там лише 3 рівні,
   хотів своє з ширшим функціоналом).
2. **Шахи** для стратегічного мислення — chess.com з «персональним ШІ-тренером», із цим усе ок
   (лишається зовнішнім).
3. **Скоромовки вголос** — практикував лише одну («лігурія»), хотів генерацію рандомних і щоразу нову.
4. **Клавогонки** (klavogonki.ru) — там розкладка лише рос./англ., хотів українську.
5. **Швидкочитання** — хотів свій інструмент, кращий за «допотопний» teoset.com/speed-read
   (там лише швидкість появи слів по 1), адаптивніший і крутіший.
6. **Арифметичний тренажер** — мав локальний html-файл, хотів покращити.
7. **Харизма** — дивиться відео на YouTube-каналі @charismaoncommand_ru.
8. **«Осознаність»** — медитує, намагається повсякденно відчувати дихання та тілесні відчуття.
9. **Англійська + ШІ-розвиток** — під це вже є 2 інші сайти, де веде прогрес (обіцяв показати).

### Цілі користувача (дослівно за змістом)

- Чудова фізична форма (регулярний спорт + правильне харчування)
- Швидкість читання **400+ сл/хв** (в ідеалі 600–800); зараз 150–180
- Швидкість друку **400+ символів/хв**; зараз ~250–300
- Усвідомлене дихання та тілесні відчуття **протягом усього часу**, не лише під час медитації
- Англійська **C1** (на перспективу C2), польська **B2**
- Навчитись засинати **до 5 хв** (буквально «відрубуватись»); зараз 20–90 хв
- Харизма прокачана так, щоб усі звертали увагу, вмів утримувати цю увагу, був справжнім лідером
- Успіхи в роботі (трекає на іншому сайті)
- Успіхи з дівчатами: **1000 підходів, 100 побачень, 20 дівчат** — на потім, зараз нема можливості
- Шахи **FIDE 1400–1600**
- Прочитати **500 книг**
- Стильний вигляд: форма + зачіска + одяг + **тату по 1 на кожне досягнення** (як символ)
- Жим **120 кг**, присід **150 кг**, станова **200 кг**, олімпійський тріатлон, **10 гір**
- Стати популярним і знаменитим (щоб впізнавали) — для того й харизма
- Заробити **10 млн $**, на піку **100 тис $/міс**
- Медитувати **3 год за раз**, загалом **1000 медитацій** (на старті вже кілька десятків)
- Навчитись готувати **50 страв** (на старті 16)
- Побудувати сім'ю та завести дітей — на перспективу, після «дівчат»

Просив також порекомендувати додаткові значущі цілі для геніальності та під усі цілі
поставити щоденні/щотижневі задачі. Готовий приділяти **по кілька годин на день**.

### «Геніальність» у розумінні користувача (дослівно за змістом)

Людина, що: **«все бачить»** (помічає дрібні деталі, як Шерлок Холмс або Роланд із «Темної вежі»
Стівена Кінга); **«все чує»** (чим би не був зайнятий — «вуха на сторожі»); **«все відчуває»**
(тілесні відчуття й дихання, як у вченнях буддистів/ченців); швидко сприймає та обробляє
інформацію (швидко читає/друкує/відповідає); має розвинуті когнітивні здібності (швидкі й
влучні рішення — звідси стратегічне мислення з шахів); високий IQ та EQ; ерудована й начитана;
впевнена в собі та амбітна; соціальна, розбирається в психології людей і себе; вміє
контролювати себе (через осознаність: розуміє свій організм, справляється з імпульсами,
стримує пориви/гнів, вміє викликати бажання до чогось); розбирається в медицині
(пізнання себе, щоб бути у формі — ментальній і фізичній).

## Контекст для тону і пріоритетів

Усі цілі заведені на сторінці «Цілі» + 12 рекомендованих від тренера (digit span 9+,
журнал Шерлока, π-100, колода карт, есе, виступи, сон 7–8 год, шахові задачі 2000,
холодний душ, жонглювання, зорова матриця, 10k слів). Щоденні/щотижневі задачі під цілі
заведені на дашборді. Шахи та харизма — свідомо зовнішні (chess.com, YouTube), на дашборді
є посилання й задачі.

## Беклог (озвучене/запропоноване, не зроблене)

- Доливати прозу батчами по 50–100 уривків у data.prose.js (узгоджено: бандл, не API;
  стеля — тисячі уривків без проблем). Варіант на майбутнє: Вікіджерела/Гутенберг як зовнішнє джерело.
- Червоно-чорні таблиці Горбова–Шульте; dual N-back; гра Кіма; тренажер π/колоди карт (мнемотехніка).
- AI-генеровані питання «по суті» для квізу читання (потрібен API-ключ — обговорити з користувачем).
- Статистика-дашборд за місяці; синхронізація між пристроями.
- Користувач хотів показати ще 2 свої сайти (англійська/ШІ і робота) — можлива інтеграція стилю.

## Нюанси

- localStorage ключ `genius.v1`; нагадувати про експорт («Дані») при великих змінах структури state.
- При зміні структури state — міграція м'яка: load() доповнює відсутні ключі з emptyState().
- favicon — emoji-SVG data URI; шрифт Nunito з Google Fonts (є системний фолбек).
- Усі коміти/PR — українською, із підсумком по модулях.
